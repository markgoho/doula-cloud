# Proof for #1466

This paragraph is hard-wrapped on purpose, at a fixed column, so that the
docs job refuses it. This PR is never merged; it exists only to show that
a docs-only PR is still gated on docs/ after api and app stop running.
