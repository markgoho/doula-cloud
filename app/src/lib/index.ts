/* eslint-disable unicorn/no-empty-file -- SvelteKit's default $lib barrel; package.json's `#lib` import maps here even though nothing imports the bare specifier yet */
// place files you want to import through the `#lib` alias in this folder.
