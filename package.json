{
  "name": "doula-cloud",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "cd hugo && hugo --minify",
    "dev": "bun run --cwd app dev:full"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "firebase-tools": "^15.28.1"
  },
  "peerDependencies": {
    "typescript": "6.0.3"
  }
}
